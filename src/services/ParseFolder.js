
function manageSelection(choosedFileToParse) {
    if (Array.isArray(choosedFileToParse)) {
        //TODO
    } else if (choosedFileToParse === null) {
        //TODO    
    } else {
        //TODO    
    }
}

const ParseFolder =  () => {
  

}

export default ParseFolder();